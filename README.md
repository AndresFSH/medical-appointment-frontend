Medical Appointment Frontend

How to run:

1. Copy .env.example to .env and set VITE_API_BASE if needed (default: http://localhost:8081)
2. npm install
3. npm run dev

This project uses React + Vite (JavaScript), react-router and a small fetch-based API client.
